# Postgres compartilhado (mesmo do core; schema public é do core, tenant_<id> é dos produtos)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/sapienza

# Porta do serviço
PORT=8081

# Segredo do JWT curto emitido pelo core (deve bater com PRODUCT_JWT_SECRET do core)
PRODUCT_JWT_SECRET=troque-por-um-segredo-forte

# AES-256-GCM p/ segredos por-tenant (32 bytes base64): openssl rand -base64 32
MARGOT_ENC_KEY=

# Evolution API (servidor global da Sapienza; instância roteia por tenant).
# EVOLUTION_API_KEY é a apikey GLOBAL — usada tanto para enviar quanto para
# criar/gerir instâncias (onboarding por QR).
EVOLUTION_API_URL=https://evolution.suaempresa.com
EVOLUTION_API_KEY=
# Segredo global do webhook (fallback enquanto um tenant não tem o seu). No
# onboarding por QR cada tenant ganha um segredo próprio, gerado automaticamente.
EVOLUTION_WEBHOOK_SECRET=

# URL pública DESTA Margot — a Margot a registra no Evolution como destino do
# webhook ao criar a instância. Sem ela, o onboarding self-serve por QR não sobe.
MARGOT_PUBLIC_URL=https://margot.suaempresa.com

# Claude (se ausente, o bot responde só com o fallback do tenant)
ANTHROPIC_API_KEY=
